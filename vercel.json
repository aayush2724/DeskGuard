{
  "buildCommand": "cd client && npm install && npm run build && cp dist/index.html dist/app.html && cp -r marketing/* dist/ && cp marketing/index.html dist/index.html",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd client && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/live", "destination": "/app.html" },
    { "source": "/scan", "destination": "/app.html" },
    { "source": "/librarian", "destination": "/app.html" }
  ]
}
